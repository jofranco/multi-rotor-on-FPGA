// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2017.1
// Copyright (C) 1986-2017 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================

/***************************** Include Files *********************************/
#include "xflightmain.h"

/************************** Function Implementation *************************/
#ifndef __linux__
int XFlightmain_CfgInitialize(XFlightmain *InstancePtr, XFlightmain_Config *ConfigPtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(ConfigPtr != NULL);

    InstancePtr->Ctrl_BaseAddress = ConfigPtr->Ctrl_BaseAddress;
    InstancePtr->IsReady = XIL_COMPONENT_IS_READY;

    return XST_SUCCESS;
}
#endif

void XFlightmain_Start(XFlightmain *InstancePtr) {
    u32 Data;

    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XFlightmain_ReadReg(InstancePtr->Ctrl_BaseAddress, XFLIGHTMAIN_CTRL_ADDR_AP_CTRL) & 0x80;
    XFlightmain_WriteReg(InstancePtr->Ctrl_BaseAddress, XFLIGHTMAIN_CTRL_ADDR_AP_CTRL, Data | 0x01);
}

u32 XFlightmain_IsDone(XFlightmain *InstancePtr) {
    u32 Data;

    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XFlightmain_ReadReg(InstancePtr->Ctrl_BaseAddress, XFLIGHTMAIN_CTRL_ADDR_AP_CTRL);
    return (Data >> 1) & 0x1;
}

u32 XFlightmain_IsIdle(XFlightmain *InstancePtr) {
    u32 Data;

    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XFlightmain_ReadReg(InstancePtr->Ctrl_BaseAddress, XFLIGHTMAIN_CTRL_ADDR_AP_CTRL);
    return (Data >> 2) & 0x1;
}

u32 XFlightmain_IsReady(XFlightmain *InstancePtr) {
    u32 Data;

    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XFlightmain_ReadReg(InstancePtr->Ctrl_BaseAddress, XFLIGHTMAIN_CTRL_ADDR_AP_CTRL);
    // check ap_start to see if the pcore is ready for next input
    return !(Data & 0x1);
}

void XFlightmain_EnableAutoRestart(XFlightmain *InstancePtr) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XFlightmain_WriteReg(InstancePtr->Ctrl_BaseAddress, XFLIGHTMAIN_CTRL_ADDR_AP_CTRL, 0x80);
}

void XFlightmain_DisableAutoRestart(XFlightmain *InstancePtr) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XFlightmain_WriteReg(InstancePtr->Ctrl_BaseAddress, XFLIGHTMAIN_CTRL_ADDR_AP_CTRL, 0);
}

void XFlightmain_Set_obj_avd_cmd_V(XFlightmain *InstancePtr, u32 Data) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XFlightmain_WriteReg(InstancePtr->Ctrl_BaseAddress, XFLIGHTMAIN_CTRL_ADDR_OBJ_AVD_CMD_V_DATA, Data);
}

u32 XFlightmain_Get_obj_avd_cmd_V(XFlightmain *InstancePtr) {
    u32 Data;

    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XFlightmain_ReadReg(InstancePtr->Ctrl_BaseAddress, XFLIGHTMAIN_CTRL_ADDR_OBJ_AVD_CMD_V_DATA);
    return Data;
}

void XFlightmain_Set_horizon_cmd_V(XFlightmain *InstancePtr, u32 Data) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XFlightmain_WriteReg(InstancePtr->Ctrl_BaseAddress, XFLIGHTMAIN_CTRL_ADDR_HORIZON_CMD_V_DATA, Data);
}

u32 XFlightmain_Get_horizon_cmd_V(XFlightmain *InstancePtr) {
    u32 Data;

    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XFlightmain_ReadReg(InstancePtr->Ctrl_BaseAddress, XFLIGHTMAIN_CTRL_ADDR_HORIZON_CMD_V_DATA);
    return Data;
}

void XFlightmain_Set_obj_avd_flag_V(XFlightmain *InstancePtr, u32 Data) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XFlightmain_WriteReg(InstancePtr->Ctrl_BaseAddress, XFLIGHTMAIN_CTRL_ADDR_OBJ_AVD_FLAG_V_DATA, Data);
}

u32 XFlightmain_Get_obj_avd_flag_V(XFlightmain *InstancePtr) {
    u32 Data;

    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XFlightmain_ReadReg(InstancePtr->Ctrl_BaseAddress, XFLIGHTMAIN_CTRL_ADDR_OBJ_AVD_FLAG_V_DATA);
    return Data;
}

u32 XFlightmain_Get_rcCmdIn_V_BaseAddress(XFlightmain *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Ctrl_BaseAddress + XFLIGHTMAIN_CTRL_ADDR_RCCMDIN_V_BASE);
}

u32 XFlightmain_Get_rcCmdIn_V_HighAddress(XFlightmain *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Ctrl_BaseAddress + XFLIGHTMAIN_CTRL_ADDR_RCCMDIN_V_HIGH);
}

u32 XFlightmain_Get_rcCmdIn_V_TotalBytes(XFlightmain *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (XFLIGHTMAIN_CTRL_ADDR_RCCMDIN_V_HIGH - XFLIGHTMAIN_CTRL_ADDR_RCCMDIN_V_BASE + 1);
}

u32 XFlightmain_Get_rcCmdIn_V_BitWidth(XFlightmain *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XFLIGHTMAIN_CTRL_WIDTH_RCCMDIN_V;
}

u32 XFlightmain_Get_rcCmdIn_V_Depth(XFlightmain *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XFLIGHTMAIN_CTRL_DEPTH_RCCMDIN_V;
}

u32 XFlightmain_Write_rcCmdIn_V_Words(XFlightmain *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XFLIGHTMAIN_CTRL_ADDR_RCCMDIN_V_HIGH - XFLIGHTMAIN_CTRL_ADDR_RCCMDIN_V_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(int *)(InstancePtr->Ctrl_BaseAddress + XFLIGHTMAIN_CTRL_ADDR_RCCMDIN_V_BASE + (offset + i)*4) = *(data + i);
    }
    return length;
}

u32 XFlightmain_Read_rcCmdIn_V_Words(XFlightmain *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XFLIGHTMAIN_CTRL_ADDR_RCCMDIN_V_HIGH - XFLIGHTMAIN_CTRL_ADDR_RCCMDIN_V_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(int *)(InstancePtr->Ctrl_BaseAddress + XFLIGHTMAIN_CTRL_ADDR_RCCMDIN_V_BASE + (offset + i)*4);
    }
    return length;
}

u32 XFlightmain_Write_rcCmdIn_V_Bytes(XFlightmain *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XFLIGHTMAIN_CTRL_ADDR_RCCMDIN_V_HIGH - XFLIGHTMAIN_CTRL_ADDR_RCCMDIN_V_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(char *)(InstancePtr->Ctrl_BaseAddress + XFLIGHTMAIN_CTRL_ADDR_RCCMDIN_V_BASE + offset + i) = *(data + i);
    }
    return length;
}

u32 XFlightmain_Read_rcCmdIn_V_Bytes(XFlightmain *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XFLIGHTMAIN_CTRL_ADDR_RCCMDIN_V_HIGH - XFLIGHTMAIN_CTRL_ADDR_RCCMDIN_V_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(char *)(InstancePtr->Ctrl_BaseAddress + XFLIGHTMAIN_CTRL_ADDR_RCCMDIN_V_BASE + offset + i);
    }
    return length;
}

u32 XFlightmain_Get_measured_V_BaseAddress(XFlightmain *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Ctrl_BaseAddress + XFLIGHTMAIN_CTRL_ADDR_MEASURED_V_BASE);
}

u32 XFlightmain_Get_measured_V_HighAddress(XFlightmain *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Ctrl_BaseAddress + XFLIGHTMAIN_CTRL_ADDR_MEASURED_V_HIGH);
}

u32 XFlightmain_Get_measured_V_TotalBytes(XFlightmain *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (XFLIGHTMAIN_CTRL_ADDR_MEASURED_V_HIGH - XFLIGHTMAIN_CTRL_ADDR_MEASURED_V_BASE + 1);
}

u32 XFlightmain_Get_measured_V_BitWidth(XFlightmain *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XFLIGHTMAIN_CTRL_WIDTH_MEASURED_V;
}

u32 XFlightmain_Get_measured_V_Depth(XFlightmain *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XFLIGHTMAIN_CTRL_DEPTH_MEASURED_V;
}

u32 XFlightmain_Write_measured_V_Words(XFlightmain *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XFLIGHTMAIN_CTRL_ADDR_MEASURED_V_HIGH - XFLIGHTMAIN_CTRL_ADDR_MEASURED_V_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(int *)(InstancePtr->Ctrl_BaseAddress + XFLIGHTMAIN_CTRL_ADDR_MEASURED_V_BASE + (offset + i)*4) = *(data + i);
    }
    return length;
}

u32 XFlightmain_Read_measured_V_Words(XFlightmain *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XFLIGHTMAIN_CTRL_ADDR_MEASURED_V_HIGH - XFLIGHTMAIN_CTRL_ADDR_MEASURED_V_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(int *)(InstancePtr->Ctrl_BaseAddress + XFLIGHTMAIN_CTRL_ADDR_MEASURED_V_BASE + (offset + i)*4);
    }
    return length;
}

u32 XFlightmain_Write_measured_V_Bytes(XFlightmain *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XFLIGHTMAIN_CTRL_ADDR_MEASURED_V_HIGH - XFLIGHTMAIN_CTRL_ADDR_MEASURED_V_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(char *)(InstancePtr->Ctrl_BaseAddress + XFLIGHTMAIN_CTRL_ADDR_MEASURED_V_BASE + offset + i) = *(data + i);
    }
    return length;
}

u32 XFlightmain_Read_measured_V_Bytes(XFlightmain *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XFLIGHTMAIN_CTRL_ADDR_MEASURED_V_HIGH - XFLIGHTMAIN_CTRL_ADDR_MEASURED_V_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(char *)(InstancePtr->Ctrl_BaseAddress + XFLIGHTMAIN_CTRL_ADDR_MEASURED_V_BASE + offset + i);
    }
    return length;
}

void XFlightmain_InterruptGlobalEnable(XFlightmain *InstancePtr) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XFlightmain_WriteReg(InstancePtr->Ctrl_BaseAddress, XFLIGHTMAIN_CTRL_ADDR_GIE, 1);
}

void XFlightmain_InterruptGlobalDisable(XFlightmain *InstancePtr) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XFlightmain_WriteReg(InstancePtr->Ctrl_BaseAddress, XFLIGHTMAIN_CTRL_ADDR_GIE, 0);
}

void XFlightmain_InterruptEnable(XFlightmain *InstancePtr, u32 Mask) {
    u32 Register;

    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Register =  XFlightmain_ReadReg(InstancePtr->Ctrl_BaseAddress, XFLIGHTMAIN_CTRL_ADDR_IER);
    XFlightmain_WriteReg(InstancePtr->Ctrl_BaseAddress, XFLIGHTMAIN_CTRL_ADDR_IER, Register | Mask);
}

void XFlightmain_InterruptDisable(XFlightmain *InstancePtr, u32 Mask) {
    u32 Register;

    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Register =  XFlightmain_ReadReg(InstancePtr->Ctrl_BaseAddress, XFLIGHTMAIN_CTRL_ADDR_IER);
    XFlightmain_WriteReg(InstancePtr->Ctrl_BaseAddress, XFLIGHTMAIN_CTRL_ADDR_IER, Register & (~Mask));
}

void XFlightmain_InterruptClear(XFlightmain *InstancePtr, u32 Mask) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XFlightmain_WriteReg(InstancePtr->Ctrl_BaseAddress, XFLIGHTMAIN_CTRL_ADDR_ISR, Mask);
}

u32 XFlightmain_InterruptGetEnabled(XFlightmain *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XFlightmain_ReadReg(InstancePtr->Ctrl_BaseAddress, XFLIGHTMAIN_CTRL_ADDR_IER);
}

u32 XFlightmain_InterruptGetStatus(XFlightmain *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XFlightmain_ReadReg(InstancePtr->Ctrl_BaseAddress, XFLIGHTMAIN_CTRL_ADDR_ISR);
}

