// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2018.2
// Copyright (C) 1986-2018 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================

#ifndef XRX_DRIVER_H
#define XRX_DRIVER_H

#ifdef __cplusplus
extern "C" {
#endif

/***************************** Include Files *********************************/
#ifndef __linux__
#include "xil_types.h"
#include "xil_assert.h"
#include "xstatus.h"
#include "xil_io.h"
#else
#include <stdint.h>
#include <assert.h>
#include <dirent.h>
#include <fcntl.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/mman.h>
#include <unistd.h>
#include <stddef.h>
#endif
#include "xrx_driver_hw.h"

/**************************** Type Definitions ******************************/
#ifdef __linux__
typedef uint8_t u8;
typedef uint16_t u16;
typedef uint32_t u32;
#else
typedef struct {
    u16 DeviceId;
    u32 Ctrl_BaseAddress;
} XRx_driver_Config;
#endif

typedef struct {
    u32 Ctrl_BaseAddress;
    u32 IsReady;
} XRx_driver;

/***************** Macros (Inline Functions) Definitions *********************/
#ifndef __linux__
#define XRx_driver_WriteReg(BaseAddress, RegOffset, Data) \
    Xil_Out32((BaseAddress) + (RegOffset), (u32)(Data))
#define XRx_driver_ReadReg(BaseAddress, RegOffset) \
    Xil_In32((BaseAddress) + (RegOffset))
#else
#define XRx_driver_WriteReg(BaseAddress, RegOffset, Data) \
    *(volatile u32*)((BaseAddress) + (RegOffset)) = (u32)(Data)
#define XRx_driver_ReadReg(BaseAddress, RegOffset) \
    *(volatile u32*)((BaseAddress) + (RegOffset))

#define Xil_AssertVoid(expr)    assert(expr)
#define Xil_AssertNonvoid(expr) assert(expr)

#define XST_SUCCESS             0
#define XST_DEVICE_NOT_FOUND    2
#define XST_OPEN_DEVICE_FAILED  3
#define XIL_COMPONENT_IS_READY  1
#endif

/************************** Function Prototypes *****************************/
#ifndef __linux__
int XRx_driver_Initialize(XRx_driver *InstancePtr, u16 DeviceId);
XRx_driver_Config* XRx_driver_LookupConfig(u16 DeviceId);
int XRx_driver_CfgInitialize(XRx_driver *InstancePtr, XRx_driver_Config *ConfigPtr);
#else
int XRx_driver_Initialize(XRx_driver *InstancePtr, const char* InstanceName);
int XRx_driver_Release(XRx_driver *InstancePtr);
#endif

void XRx_driver_Start(XRx_driver *InstancePtr);
u32 XRx_driver_IsDone(XRx_driver *InstancePtr);
u32 XRx_driver_IsIdle(XRx_driver *InstancePtr);
u32 XRx_driver_IsReady(XRx_driver *InstancePtr);
void XRx_driver_EnableAutoRestart(XRx_driver *InstancePtr);
void XRx_driver_DisableAutoRestart(XRx_driver *InstancePtr);


void XRx_driver_InterruptGlobalEnable(XRx_driver *InstancePtr);
void XRx_driver_InterruptGlobalDisable(XRx_driver *InstancePtr);
void XRx_driver_InterruptEnable(XRx_driver *InstancePtr, u32 Mask);
void XRx_driver_InterruptDisable(XRx_driver *InstancePtr, u32 Mask);
void XRx_driver_InterruptClear(XRx_driver *InstancePtr, u32 Mask);
u32 XRx_driver_InterruptGetEnabled(XRx_driver *InstancePtr);
u32 XRx_driver_InterruptGetStatus(XRx_driver *InstancePtr);

#ifdef __cplusplus
}
#endif

#endif
