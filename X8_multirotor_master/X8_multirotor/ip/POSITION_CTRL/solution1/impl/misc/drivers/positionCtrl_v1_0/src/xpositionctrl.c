// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2018.2
// Copyright (C) 1986-2018 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================

/***************************** Include Files *********************************/
#include "xpositionctrl.h"

/************************** Function Implementation *************************/
#ifndef __linux__
int XPositionctrl_CfgInitialize(XPositionctrl *InstancePtr, XPositionctrl_Config *ConfigPtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(ConfigPtr != NULL);

    InstancePtr->Ctrl_BaseAddress = ConfigPtr->Ctrl_BaseAddress;
    InstancePtr->IsReady = XIL_COMPONENT_IS_READY;

    return XST_SUCCESS;
}
#endif

void XPositionctrl_Start(XPositionctrl *InstancePtr) {
    u32 Data;

    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XPositionctrl_ReadReg(InstancePtr->Ctrl_BaseAddress, XPOSITIONCTRL_CTRL_ADDR_AP_CTRL) & 0x80;
    XPositionctrl_WriteReg(InstancePtr->Ctrl_BaseAddress, XPOSITIONCTRL_CTRL_ADDR_AP_CTRL, Data | 0x01);
}

u32 XPositionctrl_IsDone(XPositionctrl *InstancePtr) {
    u32 Data;

    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XPositionctrl_ReadReg(InstancePtr->Ctrl_BaseAddress, XPOSITIONCTRL_CTRL_ADDR_AP_CTRL);
    return (Data >> 1) & 0x1;
}

u32 XPositionctrl_IsIdle(XPositionctrl *InstancePtr) {
    u32 Data;

    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XPositionctrl_ReadReg(InstancePtr->Ctrl_BaseAddress, XPOSITIONCTRL_CTRL_ADDR_AP_CTRL);
    return (Data >> 2) & 0x1;
}

u32 XPositionctrl_IsReady(XPositionctrl *InstancePtr) {
    u32 Data;

    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XPositionctrl_ReadReg(InstancePtr->Ctrl_BaseAddress, XPOSITIONCTRL_CTRL_ADDR_AP_CTRL);
    // check ap_start to see if the pcore is ready for next input
    return !(Data & 0x1);
}

void XPositionctrl_EnableAutoRestart(XPositionctrl *InstancePtr) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XPositionctrl_WriteReg(InstancePtr->Ctrl_BaseAddress, XPOSITIONCTRL_CTRL_ADDR_AP_CTRL, 0x80);
}

void XPositionctrl_DisableAutoRestart(XPositionctrl *InstancePtr) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XPositionctrl_WriteReg(InstancePtr->Ctrl_BaseAddress, XPOSITIONCTRL_CTRL_ADDR_AP_CTRL, 0);
}

u32 XPositionctrl_Get_pmod_data_BaseAddress(XPositionctrl *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Ctrl_BaseAddress + XPOSITIONCTRL_CTRL_ADDR_PMOD_DATA_BASE);
}

u32 XPositionctrl_Get_pmod_data_HighAddress(XPositionctrl *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Ctrl_BaseAddress + XPOSITIONCTRL_CTRL_ADDR_PMOD_DATA_HIGH);
}

u32 XPositionctrl_Get_pmod_data_TotalBytes(XPositionctrl *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (XPOSITIONCTRL_CTRL_ADDR_PMOD_DATA_HIGH - XPOSITIONCTRL_CTRL_ADDR_PMOD_DATA_BASE + 1);
}

u32 XPositionctrl_Get_pmod_data_BitWidth(XPositionctrl *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XPOSITIONCTRL_CTRL_WIDTH_PMOD_DATA;
}

u32 XPositionctrl_Get_pmod_data_Depth(XPositionctrl *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XPOSITIONCTRL_CTRL_DEPTH_PMOD_DATA;
}

u32 XPositionctrl_Write_pmod_data_Words(XPositionctrl *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XPOSITIONCTRL_CTRL_ADDR_PMOD_DATA_HIGH - XPOSITIONCTRL_CTRL_ADDR_PMOD_DATA_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(int *)(InstancePtr->Ctrl_BaseAddress + XPOSITIONCTRL_CTRL_ADDR_PMOD_DATA_BASE + (offset + i)*4) = *(data + i);
    }
    return length;
}

u32 XPositionctrl_Read_pmod_data_Words(XPositionctrl *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XPOSITIONCTRL_CTRL_ADDR_PMOD_DATA_HIGH - XPOSITIONCTRL_CTRL_ADDR_PMOD_DATA_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(int *)(InstancePtr->Ctrl_BaseAddress + XPOSITIONCTRL_CTRL_ADDR_PMOD_DATA_BASE + (offset + i)*4);
    }
    return length;
}

u32 XPositionctrl_Write_pmod_data_Bytes(XPositionctrl *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XPOSITIONCTRL_CTRL_ADDR_PMOD_DATA_HIGH - XPOSITIONCTRL_CTRL_ADDR_PMOD_DATA_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(char *)(InstancePtr->Ctrl_BaseAddress + XPOSITIONCTRL_CTRL_ADDR_PMOD_DATA_BASE + offset + i) = *(data + i);
    }
    return length;
}

u32 XPositionctrl_Read_pmod_data_Bytes(XPositionctrl *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XPOSITIONCTRL_CTRL_ADDR_PMOD_DATA_HIGH - XPOSITIONCTRL_CTRL_ADDR_PMOD_DATA_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(char *)(InstancePtr->Ctrl_BaseAddress + XPOSITIONCTRL_CTRL_ADDR_PMOD_DATA_BASE + offset + i);
    }
    return length;
}

void XPositionctrl_InterruptGlobalEnable(XPositionctrl *InstancePtr) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XPositionctrl_WriteReg(InstancePtr->Ctrl_BaseAddress, XPOSITIONCTRL_CTRL_ADDR_GIE, 1);
}

void XPositionctrl_InterruptGlobalDisable(XPositionctrl *InstancePtr) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XPositionctrl_WriteReg(InstancePtr->Ctrl_BaseAddress, XPOSITIONCTRL_CTRL_ADDR_GIE, 0);
}

void XPositionctrl_InterruptEnable(XPositionctrl *InstancePtr, u32 Mask) {
    u32 Register;

    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Register =  XPositionctrl_ReadReg(InstancePtr->Ctrl_BaseAddress, XPOSITIONCTRL_CTRL_ADDR_IER);
    XPositionctrl_WriteReg(InstancePtr->Ctrl_BaseAddress, XPOSITIONCTRL_CTRL_ADDR_IER, Register | Mask);
}

void XPositionctrl_InterruptDisable(XPositionctrl *InstancePtr, u32 Mask) {
    u32 Register;

    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Register =  XPositionctrl_ReadReg(InstancePtr->Ctrl_BaseAddress, XPOSITIONCTRL_CTRL_ADDR_IER);
    XPositionctrl_WriteReg(InstancePtr->Ctrl_BaseAddress, XPOSITIONCTRL_CTRL_ADDR_IER, Register & (~Mask));
}

void XPositionctrl_InterruptClear(XPositionctrl *InstancePtr, u32 Mask) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XPositionctrl_WriteReg(InstancePtr->Ctrl_BaseAddress, XPOSITIONCTRL_CTRL_ADDR_ISR, Mask);
}

u32 XPositionctrl_InterruptGetEnabled(XPositionctrl *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XPositionctrl_ReadReg(InstancePtr->Ctrl_BaseAddress, XPOSITIONCTRL_CTRL_ADDR_IER);
}

u32 XPositionctrl_InterruptGetStatus(XPositionctrl *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XPositionctrl_ReadReg(InstancePtr->Ctrl_BaseAddress, XPOSITIONCTRL_CTRL_ADDR_ISR);
}

